 
 <%= form_for [@user], :html => { :class => 'form-horizontal' } do |f| %>

      <% if @user.errors.any? %>

        <div id="error_explanation">

          <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>


          <ul>

          <% @user.errors.full_messages.each do |msg| %>

            <li><%= msg %></li>

          <% end %>

          </ul>

        </div>

      <% end %>


<hr>

      <div class="control-group">
         <%= f.label :username, :class => "control-label", :for => "inputEmail" %>
        <div class="controls">  
        <%= f.text_field :name, :class => "input-xlarge" ,:id=> "inputEmail", :placeholder => "Enter username"  %>
        </div>
      </div>

      <div class="control-group">
        <%= f.label :email, :class => "control-label", :for => "inputEmail" %>
        <div class="controls"> 
        <%= f.text_field :email, :class => "input-xlarge" , :id=> "inputEmail", :placeholder => "Enter email" %>
        </div>
      </div>

      <div class="control-group">
        <%= f.label :password, :class => "control-label", :for => "inputPassword" %>
        <div class="controls"> 
        <%= f.password_field :password, :class => "input-xlarge" , :id=> "inputPassword", :placeholder => "Enter password" %>
        </div>
      </div>

      <div class="control-group">
        <%= f.label :password_confirmation, :class => "control-label", :for => "inputPassword" %>
        <div class="controls"> 
        <%= f.password_field :password_confirmation, :class => "input-xlarge" , :id=> "inputPassword", :placeholder => "Enter password confirmation" %>
        </div>
      </div>

      <div class="actions">

        <%= f.submit "Create user", :class => 'btn btn-info' %>

      </div>

    <% end %>
